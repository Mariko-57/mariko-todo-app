.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

  = form_with(model: @comment, url: board_task_comments_path(@board.id, @comment.task), local: true) do |f|
    %div
      = f.label :content, 'Comment'
      %div
        = f.text_area :content
      = f.submit 'Sunbmit', class: 'btn-primary'